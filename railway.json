{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd apps/web && npm ci && npx prisma generate && npx next build"
  },
  "deploy": {
    "startCommand": "cd apps/web && npx prisma db push && npm run start",
    "restartPolicyType": "ON_FAILURE"
  }
}
